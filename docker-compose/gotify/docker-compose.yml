services:
  gotify:
    image: gotify/server:latest
    container_name: gotify
    restart: always
    ports:
      - "8086:80"
    volumes:
      - gotify-data:/app/data
    networks:
      - proxy

volumes:
  gotify-data:

networks:
  proxy:
    external: true
